import { ACTIVE } from 'core/src/common/variables'

/**
 * CONSTANT VARIABLES ==========================================================
 * =============================================================================
 */

export const NAME = `${ACTIVE.SERVICE}_NAME` // Namespace this module
export const MODEL_NAME = 'Name' // db Model name (Capitalized)
