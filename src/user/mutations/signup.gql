mutation signup($email: Email!, $password: Password!, $referrer: KeyValInput) {
    signup(email: $email, password: $password, referrer: $referrer) {
        id
        name
        email
        lastLogin
        referrer {
            key
            val
        }
    }
}
