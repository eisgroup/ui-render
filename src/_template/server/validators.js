/**
 * GRAPHQL VALIDATION HELPERS ==================================================
 * Validates GraphQL requests - returns error Response if invalid, else void
 * =============================================================================
 */
